# Examples from the language specification

name = "Ada"
sum = 3 + 4
msg = "Hello, ${name}. 3 + 4 = ${sum}"

s = "foo bar"
length = s::length()
words = s::split()

csv = "a,b,c"
parts = csv::split(",")

xs = [10, 20, 30, 40]
first = xs[0]
last = xs[-1]
slice = xs[1:3]

m = { name: "Ada", "age": 37 }
person_name = m:name
age = m["age"]

k = "name"
lookup = m[k]