import std:println

a = 1
b = 2

add = |x, y| {
    return x + y
}

c = add(a, b)

loop {
    match c {
        10 => { break },
    }
    c++
}

loop through 0..c with x {
    println(x)
}

plus = "${a} plus ${b} does indeed equal ${c}"
minus = "${a} minus ${b} equals ${a - b}"
