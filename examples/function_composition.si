import std:json
import std:println

data = '{"nums":[1,2,3]}' |> json:parse

inc = |x| x + 1
double = |x| x * 2
transform = inc >> double

data:nums
    |> |xs| xs::map(transform)::sum()
    |> println  # 18

