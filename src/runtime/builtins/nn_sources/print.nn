# std:print function - writes text to a stream and returns bytes written
export {
    print: |text, out = nil| {
        import std:io
        stream = match out {
            nil => io:stdout,
            _ => out,
        }
        text_str = text::to_string()
        return stream::write(text_str)
    }
}


