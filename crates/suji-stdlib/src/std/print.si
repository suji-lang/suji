# std:print function - writes text to a stream and returns bytes written
import std:io

export |text, out = nil| {
    stream = match out {
        nil => io:stdout,
        _ => out,
    }

    stream::write(text::to_string())
}
