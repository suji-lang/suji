# std:println function - writes text followed by newline
import std:io

export |text = "", out = nil| {
    stream = match out {
        nil => io:stdout,
        _ => out,
    }

    stream::write(text::to_string() + "\n")
}
