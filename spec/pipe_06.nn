import std:println
import std:io

sink = || {
    return || {
        line = io:stdin::read_line()
        match line {
            nil: "empty"
            _: line
        }
    }
}

out = `printf ''` | sink()
println(out)  # empty
