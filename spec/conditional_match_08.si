import std:println

# Nested conditional match
x = 3
y = 4
result = match {
    x > 0 => match {
        y > 0 => "both positive",
        _ => "x positive, y non-positive",
    },
    _ => "x non-positive",
}

println(result)  # both positive
