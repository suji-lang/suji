import std:println

source = || {
    return || {
        println("alpha")
        println("beta")
        println("gamma")
    }
}

out = source() | `grep beta`
println(out::replace("\n", "\\n"))  # beta\n
