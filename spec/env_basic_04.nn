# Test env:var colon assignment
import std:env:var
import std:println

# Try colon assignment
var:TEST_VAR_COLON = "hello"

# Try to read it back with get method
println(var::get("TEST_VAR_COLON", "not_found"))  # hello
