# Test env:var keys method
import std:env:var
import std:println

# Set a test variable
var:TEST_KEYS = "test"

# Get all keys and check if our test key is in there
keys = var::keys()
println(keys::contains("TEST_KEYS"))  # true
