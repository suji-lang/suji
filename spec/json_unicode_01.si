import std:println
import std:json

# JSON with unicode characters
json_str = "{\"message\": \"Hello World\", \"emoji\": \"rocket\"}"
data = json:parse(json_str)

println(data:message)  # Hello World
