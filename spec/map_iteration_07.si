import std:println

# Test map iteration with control flow
data = { a: 1, b: 2, c: 3, d: 4 }
result = ""

loop through data with k, v {
    match k {
        "c" => { break },
        _ => result = result + k,
    }
}

println(result)  # ab
