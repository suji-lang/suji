# Test env:var to_list method
import std:env:var
import std:println

# Set a test variable
var:TEST_TO_LIST = "test"

# Convert to list and check length is greater than 0
env_list = var::to_list()
println(env_list::length() > 0)  # true
