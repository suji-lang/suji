# Test env:var bracket assignment
import std:env:var
import std:println

# Try bracket assignment
var["TEST_VAR_BRACKET"] = "hello"

# Try to read it back with get method
println(var::get("TEST_VAR_BRACKET", "not_found"))  # hello
