# Test ENV bracket assignment
import std:ENV
import std:println

# Try bracket assignment
ENV["TEST_VAR_BRACKET"] = "hello"

# Try to read it back with get method
println(ENV::get("TEST_VAR_BRACKET", "not_found"))  # hello
