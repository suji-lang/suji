import std:println
import std:json

# Error handling - test that valid JSON works
json_str = "{\"name\": \"Alice\", \"age\": 30}"
data = json:parse(json_str)

# If we get here, parsing worked
println(data:name)  # Alice
