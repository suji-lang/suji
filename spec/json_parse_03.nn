import std:println
import std:json

# JSON parse - nested structure
json_str = "{\"users\": [{\"name\": \"Alice\"}, {\"name\": \"Bob\"}], \"count\": 2}"
data = json:parse(json_str)

println(data:count)  # 2
