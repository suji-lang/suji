import std:println

# Test complex nested wildcard patterns
complex = ((1, 2, 3), (4, 5, 6), (7, 8, 9))
result = match complex {
    ((_, _, _), (4, _, _), (_, _, _)) => "complex nested match",
    _ => "no match",
}

println(result)  # complex nested match
